name: OpAlert
main: com.cyprias.opalert.OpAlert
version: 1.2.0

permissions:
    opalert.login:
        description: Notifies players with this permission when a Op logs in without this permission.
        default: false